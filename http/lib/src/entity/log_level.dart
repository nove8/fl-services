/// The verbosity level for HTTP request/response logging.
enum LogLevel {
  /// Disables all logging output.
  none,

  /// Enables full logging of HTTP traffic including method, URL, headers, and body.
  all,
}
